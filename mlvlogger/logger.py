
class Logger:
    """
    Logger is the entry class for the logging system.
    """

    def __init__(self, experiment_path, script_name):
        """
        experiment_path: the path to the experiment results folder
        script_name: the name of the script producing this log
        """
        self.path = experiment_path
        self.script_name = sn

    
